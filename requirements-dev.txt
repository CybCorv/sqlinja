# Dev-only dependencies for tests/test_mysql_docker.py and
# tests/test_mssql_docker.py. The library itself has zero dependencies;
# these are only needed to run the dialect tests against real MySQL/MSSQL
# instances (see docker run commands in those files' docstrings).
mysql-connector-python
pymssql
